/**
 * Only execute if jQuery is defined.
 */

 if (jQuery) {
    (function ($, undefined) {

        'use strict';

        $(function () {
            main();
        });

        /**
         * Main function
         */
        function main() {
          // Your code goes here
        }
    })(jQuery);
 }